import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowUpRight, BrainCircuit, Eye, LockKeyhole, ScanText } from "lucide-react";
import { GlassButton, Page, SectionHeader } from "@/components/core";
import { useApplication } from "@/context/ApplicationContext";
import { copy } from "@/lib/i18n";
export const Route=createFileRoute("/about")({head:()=>({meta:[{title:"About MUSTAWFI — Responsible Loan Intelligence"},{name:"description",content:"How MUSTAWFI combines computer vision, human verification, and machine learning."},{property:"og:title",content:"About MUSTAWFI"},{property:"og:description",content:"Computer vision, human verification, and responsible machine-learning prediction."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:About});
function About(){const app=useApplication();const t=copy[app.language].about;const icons=[Eye,ScanText,BrainCircuit,LockKeyhole];return <Page><div className="shell pb-28"><SectionHeader eyebrow={t.eyebrow} title={t.title} body={t.body}/><div className="mt-20 grid gap-px overflow-hidden border-y border-border bg-border md:grid-cols-2">{t.items.map(([title,body],i)=>{const Icon=icons[i]??Eye;return <section key={title} className="bg-background p-8 md:p-12"><Icon className="size-6"/><h2 className="mt-12 text-2xl font-semibold">{title}</h2><p className="mt-4 max-w-lg leading-7 text-muted-foreground">{body}</p></section>})}</div><GlassButton asChild className="mt-12"><Link to="/analyze">{copy[app.language].hero.primary}<ArrowUpRight/></Link></GlassButton></div></Page>}